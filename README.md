# ariadne-relay-subscriptions-notifications

## Ariadne API server

``` sh
cd ariadne-api-server
python3 -m venv venv
source venv/bin/activate
pip install -r requirements-lock.txt
uvicorn example:app
```

